@page "/Listing"
@using Core.Models
<h3>Listings</h3>

@*<EditForm EditContext="@aEditContext" OnSubmit="@handleSubmit" class="row p-3">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <div class="col-md-12 mb-3">
        <label for="Name">Name</label>
        <InputText id="Name" @bind-Value="AListing.Name" class="form-control" />
    </div>
    <div class="col-md-6 mb-3">
        <label for="Price">Price</label>
        <InputNumber id="Price" @bind-Value="AListing.Price" class="form-control" />
    </div>
    <div class="col-md-6 mb-3">
        <label for="PublishedDate">Published Date</label>
        <InputDate id="PublishedDate" @bind-Value="AListing.PublishedDate" class="form-control" />
    </div>
    <div class="col-md-12 mb-3">
        <label for="Description">Description</label>
        <InputTextArea id="Description" @bind-Value="AListing.Description" class="form-control" />
    </div>

    <div class="col-md-6 mb-3">
        <label for="IsPublished">Is published</label>
        <InputCheckbox id="IsPublished" @bind-Value="AListing.IsPublished" class="form-check-input" />
    </div>

    <div class="col-md-6 mb-3">
        <label for="Category">Category</label>
        <InputSelect id="Category" @bind-Value="AListing.Category" class="form-control">
            @foreach (var anOption in categories)
            {
                <option value="@anOption">@anOption</option>
            }
        </InputSelect>
    </div>

    <div class="col-12 mb-3">
        <button type="submit" class="btn btn-primary">Submit</button>
    </div>
</EditForm>*@

@code {
    private string[] categories = { "Computer", "Audio", "Video", "Other" };


    private Listing AListing = new Listing();

    private EditContext? aEditContext;

    protected override void OnInitialized()
    {
        aEditContext = new EditContext(AListing);
    }

    private void handleSubmit()
    {

    }
}